# v1.5 Task Brief — Dayne Tang

**Role:** User Guide update + PDF export  
**Deadline:** Thu Apr 2 23:59 (buffer: Apr 9)

---

## What You Need To Do

### 1. Update User Guide text (`docs/UserGuide.md`)

Screenshots have been auto-generated by `ScreenshotTest.java` (Randall's task) and saved to `docs/images/`. Your job is to update the UG text to reference those images and ensure accuracy.

**Replace old/placeholder screenshots** with the new ones by updating image tags to reference these files:
- `startup.png` — initial app state
- `list-result.png` — after `list` command
- `add-result.png` — after `add` command
- `list-after-add.png` — list after adding a student
- `find-name-result.png` — after `find n/...`
- `find-payment-result.png` — after `find ps/...`
- `edit-result.png` — after `edit` command
- `mark-result.png` — after `mark` command
- `remark-result.png` — after `remark` command
- `view-result.png` — after `view` command
- `delete-result.png` — after `delete` command
- `help-result.png` — after `help` command
- `Ui.png` — used as the overview screenshot on the landing page

### 2. Verify `mark` command section

The `mark` command is now implemented. Verify the UG section for `mark` (around line 183–195) accurately describes:
- Format: `mark INDEX ps/PAYMENT_STATUS`
- Valid values: `Paid`, `Due`, `Overdue` (case-insensitive)
- Example: `mark 1 ps/Paid`

### 3. Add Known Limitations / Notes section

Add this near the top of the UG (e.g. after the Introduction, before the Quick Start):

```markdown
## Notes for Users

* **Launching the app:** Use `java -jar TutorCentral.jar` in a terminal rather than double-clicking the file, as
  double-clicking may not work on some systems.
* **Folder permissions:** The app cannot run correctly in a write-protected folder. Place the JAR in a folder
  where your user account has write access.
* **macOS fullscreen:** macOS users running the app in fullscreen mode may experience unexpected behaviour
  when opening secondary dialogs (e.g. the Help window). Use windowed mode instead.
```

### 4. Confirm command summary table

Ensure the command summary table at the bottom of the UG includes the `mark` command:

| Command | Format | Example |
|---------|--------|---------|
| Mark | `mark INDEX ps/PAYMENT_STATUS` | `mark 1 ps/Paid` |

### 5. Export UG as PDF

Follow the instructions in `docs/Documentation.md` to export the UG as PDF.
Save it as `TutorCentral-UserGuide.pdf` (or similar — avoid spaces/special characters).
You'll need to attach it to the v1.5 GitHub Release later (Yi Zhong's task).

---

## Alpha Testing

You must also submit at least **5 issues** labelled `alpha-bug` tagged to the **v1.5 milestone**.

Test using: `java -jar addressbook.jar` (from `build/libs/` after `./gradlew shadowJar`)

Suggested test areas:
- All commands in the UG — does the output match what the UG says?
- Invalid inputs for each command (wrong types, out-of-range index, missing fields)
- Persistence: add a student, close the app, reopen — is the student still there?
- UI: does the person list update immediately after every command?
